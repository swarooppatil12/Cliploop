import 'package:flutter/material.dart';

class AppColors {
  // Dark background
  static const Color background = Color(0xFF0A0A0F);
  static const Color surface = Color(0xFF12121A);
  static const Color surfaceElevated = Color(0xFF1C1C28);
  static const Color surfaceBorder = Color(0xFF2A2A3D);

  // Brand accents
  static const Color primaryGreen = Color(0xFF00F5A0);
  static const Color primaryPurple = Color(0xFF7B61FF);
  static const Color accentPink = Color(0xFFFF61DC);
  static const Color accentOrange = Color(0xFFFF8C42);

  // Stems (Spleeter)
  static const Color stemVocals = Color(0xFF00F5A0);
  static const Color stemAccompaniment = Color(0xFF7B61FF);
  static const Color stemDrums = Color(0xFFFF61DC);
  static const Color stemBass = Color(0xFF7B61FF);
  static const Color stemOther = Color(0xFFFF8C42);
  static const Color stemPiano = Color(0xFF61D4FF);
  static const Color stemGuitar = Color(0xFFFFD561);

  // Segment colours (Structure — both methods)
  static const Color segmentPrelude = Color(0xFF7B61FF);
  static const Color segmentInterlude = Color(0xFFFF8C42);
  static const Color segmentPostlude = Color(0xFFFFD561);

  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFF8A8A9E);
  static const Color textDisabled = Color(0xFF3A3A52);
}
